// Write a C program to accept a string from the user and display it.
#include <stdio.h>
int main()
{
    char arr[50];
    printf("Enter the string : \n");
    scanf("%s", arr);
    printf("%s", arr);
}